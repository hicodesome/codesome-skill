You are the implementation executor for task 20260813-merge-058.

Read this file completely before acting:
.agent/tasks/20260813-merge-058/HANDOFF.md

The HANDOFF is the sole source of task facts. Do not reconstruct the task from
chat or duplicate it elsewhere. Directly implement the task; do not stop at a
plan or advice.

Protocol:

1. Verify the repository facts in HANDOFF before editing.
2. Implement every acceptance criterion within the stated constraints and
   non-goals.
3. Continuously update HANDOFF progress, decisions, changed files, validation
   results, blockers, resume instructions, updated_at, and final result.
4. The runner prewrites a provisional failed result and adds a
   `runner_sentinel` field to HANDOFF before launch. This is a transport
   safeguard, not a prior task outcome. Keep the sentinel until terminal
   artifacts are ready. At terminal completion, remove `runner_sentinel`, set
   HANDOFF status to exactly success, blocked, or failed, and atomically replace
   .agent/tasks/20260813-merge-058/result.json with the matching result.
5. Do not ask the user interactive questions.
6. Use blocked only when a specific external action can unlock progress. Record
   where, attempts, evidence, who must do what, and the exact resume point.
7. Use failed when no exact external unblock exists, implementation or tests
   still fail, execution is going in the wrong direction, or the protocol
   cannot be satisfied. Preserve useful edits and evidence.
8. Use success only after all requested modifications are complete, every
   specified validation command actually ran and passed, every acceptance
   checkbox is satisfied, and no undeclared known issue remains. A success
   result must set `recommended_next_action` to exactly `orchestrator_verify`
   and record every validation command with result `passed`.
9. Do not trust your final response as task output. The terminal HANDOFF and
   result.json are mandatory even when the task is blocked or failed.
10. Self-check before finishing: after writing terminal result.json, run
    `python3 scripts/verify_result.py --repo . --task-id 20260813-merge-058` and fix
    any reported errors (schema, status, validation records) before exiting.
    If that command is not runnable in your sandbox, state it in HANDOFF.
11. Do not run git push, publish, deploy, delete credentials, modify user-global
    configuration, or broaden the provided permissions.

Visual Evidence:

- When HANDOFF has a `# Visual Evidence` section, open every listed image,
  inspect it with your vision capability, and write the recognition result for
  each image as text into result.json `observations` (one entry per image:
  `path` plus `observation`). Observations must cite concrete content (OCR
  text, numbers, coordinates, filenames) so Pi can cross-check them; never
  write unverifiable statements such as "image looks normal".
- An executor without vision cannot satisfy this contract: declare blocked or
  failed rather than skipping the images.

The result file must conform to:
.agent/tasks/20260813-merge-058/result.schema.json
